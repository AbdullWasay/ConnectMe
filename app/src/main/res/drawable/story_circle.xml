<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Define the border color and width -->
    <stroke android:width="14dp" android:color="#6B4423"/> <!-- Dark brown shade -->

    <!-- Background color inside the border -->
    <solid android:color="@android:color/transparent"/>

    <!-- Ensure padding is minimal -->
    <padding android:left="0dp" android:right="0dp" android:top="0dp" android:bottom="0dp"/>
</shape>
